require 'spec_helper'
<% sites.each do |s| %>
require '<%= s.underscore %>'
<% end %>

describe "<%= name.camelize %>Page" do
  it "should fail because you haven't written any tests yet" do
    false.should be_true
  end
end